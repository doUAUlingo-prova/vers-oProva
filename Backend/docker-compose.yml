services:
  postgres:
    image: postgres:16
    container_name: douaulingo_postgres

    environment:
      POSTGRES_DB: douaulingo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5434:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: